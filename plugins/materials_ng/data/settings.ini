#Fresnel effect, by default 1.0
fresnel_coeff 0.7
#Global reflections, when 1 is using ini file from main objects folder if no ini file is found withing livery
#when "0" won't render reflections if livery has no ini file
global_refs 1
